import { RedAddressDirective } from './red-address.directive';

describe('RedAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new RedAddressDirective();
    expect(directive).toBeTruthy();
  });
});
